package hmrc.stepDefs

class TestStepDef extends Steps {

  //  private var driver: WebDriver = _
  //  Before() {
  //    driver = new ChromeDriver()
  //  }
  //  After() {
  //    driver.close()
  //  }
  When("""^I search for "([^"]*)"$"""){ (arg0:String) =>
    //// Write code here that turns the phrase above into concrete actions

  }

}
